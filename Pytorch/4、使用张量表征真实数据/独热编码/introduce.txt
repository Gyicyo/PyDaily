独热编码是一种数据预处理技术，主要用于将分类变量（具有离散特征）转换成便于机器学习算法理解和处理的数值形式。
具体而言，将每一个类别值表示为一个二进制向量（又称位向量），向量长度等于所有类别数量，且仅有一个元素值为1（称为“热”位），其余元素为0。这种编码方式确保了不同类别之间互斥且等距，消除了类别间的隐含顺序关系。


例如，针对一组特征[A,B,C]
独热编码：
A:(1,0,0)
B:(0,1,0)
C:(0,0,1)
三者互斥且等距，无隐含顺序关系

直接映射：
A:1
B:2
C:3
三者不等距，且有隐含顺序关系，机器会认为C>B>A

