当一个函数中有yield关键词时，这个方法就不再是个普通函数，而是一个生成器函数

生成器函数可以抛出值和接收值

例如：p = yield i
抛出值为i，接收的值会赋给p
要给p赋值，必须先获取到p的上下文，因此，只有代码执行到这一行才能对p赋值（也因此生成器必须先执行一次next才能使用send传递数据）

生成器总是在yield i时结束，在p = yield时开始（如果没有赋值就直接在抛出语句后开始，没有抛出就抛出None后结束）
