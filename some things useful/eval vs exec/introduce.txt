eval和exec都是python的全局函数
用来执行str中的代码

eval只能执行一个指令，不能执行赋值等，且可以有返回（例如执行3+4后会返回一个7）
exec可以执行多个指令（也许可以是一个py文件的量？），支持赋值、导入等各种操作（编译器无法检测这种导入，代码上会报错，但是是可以正常执行的），可以视为将代码转为了字符串然后执行。

需要注意的是，exec声明的变量在执行完后会继续存在，可能会造成一些变量值莫名其妙改变了的问题